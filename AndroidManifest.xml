<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wesley.todaynews"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.wesley.todaynews.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.PageIndicatorDefaults" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 新闻详情页面 -->
        <activity android:name=".activity.NewsDetailActivity" />
        <!-- 收藏页面 -->
        <activity android:name=".activity.FavouriteActivity" />
        <!-- 登录页面 -->
        <activity android:name=".activity.LoginActivity" />
        <!-- 天气页面 -->
        <activity android:name=".activity.WeatherActivity" />
        <!-- 注册页面 -->
        <activity android:name=".activity.RegisterActivity" />
        <!-- 新闻搜索页面 -->
        <activity android:name=".activity.NewsSearchActivity" />
        <!-- 新闻搜索详情页面 -->
        <activity android:name=".activity.SearchNewsDetailActivity" />
        <!-- 组图新闻详情页 -->
        <activity android:name=".activity.PhotosDetailActivity" />
    </application>

</manifest>